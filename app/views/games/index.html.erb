<h1>Games#index</h1>
<h2>Start</h2> <!--Button to start game-->

<%= link_to "Sign Out", destroy_user_session_path, :method => :delete %><br />

<%= link_to "Leader Board", scores_index_path %>

<ul>
<%= @states.each do |state| %>
  <li><%= state.capital  %> is the capital of <%= state.state  %></li>
<% end %>
</ul>
